# 給油記録・燃費表示機能 仕様書

## 画面構成

### 1. ダッシュボード

「自分のバイクがいかに経済的か」を一目で感じられる画面。

**表示内容：**

- 当月平均燃費（km/L）
- 累積平均燃費 avg(H)（km/L）
- 給油ごとの燃費推移グラフ
- 給油履歴カード（走行距離、給油量、燃費）

---

### 2. 給油記録入力

#### 入力項目

| 項目 | 必須/任意 | 備考 |
|------|-----------|------|
| 日付・時刻 | 必須 | datetime picker、デフォルトは現在時刻 |
| 現在ODO | 必須 | 手動入力、時間位置に基づくバリデーション |
| 給油量（L） | 必須 | リットル直接入力 |
| 満タン給油 | 必須 | ON/OFF toggle |
| 記録忘れがある | 条件付き | 満タン=ONの場合のみ有効 |

---

## 満タン法（Full Tank Method）の解説

### 基本原理

燃費を正確に計算するには「消費した燃料」を知る必要がある。しかし、給油時に入れた燃料と消費した燃料は同じではない。

**満タン法**は、満タンから次の満タンまでの間に入れた燃料＝消費した燃料という原理を利用する。

```
満タン時: タンク = FULL
     ↓ 走行（燃料消費）
     ↓ 途中給油（部分給油）
     ↓ 走行（燃料消費）
次の満タン時: タンク = FULL

→ 入れた燃料の合計 = 消費した燃料
```

---

### 数学的証明

#### パターン: 満タン → 部分 → 部分 → ... → 満タン

```
時点0: 満タン (ODO = O₀, タンク = FULL)
時点1: 部分給油 f₁ L (ODO = O₁)
時点2: 部分給油 f₂ L (ODO = O₂)
...
時点n: 部分給油 fₙ L (ODO = Oₙ)
時点N: 満タン給油 fₙ₊₁ L (ODO = Oₙ₊₁)
```

#### 燃料消費の関係

各区間での燃料消費を c₁, c₂, ..., cₙ₊₁ とする：

```
c₁ = 時点0→1での消費
c₂ = 時点1→2での消費
...
cₙ₊₁ = 時点n→N での消費
```

#### タンク残量の追跡

```
時点0後: タンク = FULL
時点1後: タンク = FULL - c₁ + f₁
時点2後: タンク = FULL - c₁ + f₁ - c₂ + f₂
...
時点n後: タンク = FULL - Σcᵢ + Σfᵢ (i=1..n)
時点N後: タンク = FULL - Σcᵢ + Σfᵢ - cₙ₊₁ + fₙ₊₁ = FULL
```

#### 方程式

```
FULL - Σcᵢ + Σfᵢ - cₙ₊₁ + fₙ₊₁ = FULL
```

整理すると：

```
Σcᵢ + cₙ₊₁ = Σfᵢ + fₙ₊₁
```

つまり：

```
総消費燃料 = 総給油燃料
```

**∴ 満タンから満タンまでの給油量合計 = 消費燃料**

---

### 燃費計算式

```
燃費 = 総走行距離 / 総消費燃料
     = (Oₙ₊₁ - O₀) / (f₁ + f₂ + ... + fₙ + fₙ₊₁)
     = ΔODO / Σfuel
```

---

### 具体例

```
#1: ODO=1000, 3L, 満タン → 燃費 = (1000-0)/3 = 33.3 km/L
#2: ODO=1050, 1L, 部分   → — (tích lũy: 1L)
#3: ODO=1100, 0.5L, 部分 → — (tích lũy: 1.5L)
#4: ODO=1150, 1L, 部分   → — (tích lũy: 2.5L)
#5: ODO=1250, 3L, 満タン → 燃費 = (1250-1000)/(1+0.5+1+3) = 250/5.5 = 45.5 km/L
```

**解説:**
- #1で満タン、ODO=1000が基準点
- #2,#3,#4は部分給油 → 燃費計算せず、燃料を積み上げ
- #5で満タン → 基準点からの総走行距離/総燃料で計算

---

## 記録の編集・削除ロジック

### 設計思想: シンプルさ優先

従来の設計では、記録を削除した際に：
1. 隣接する記録に影響があるかチェック
2. 燃費の妥当性を自動チェック（カタログ値との比較）
3. 異常があれば自動でフラグを付与

という複雑なロジックが必要だった。

**MVP では、この複雑さを排除し、シンプルなアプローチを採用:**

### 採用したアプローチ

```
データ変更（追加/編集/削除）
    ↓
日時でソート
    ↓
全レコードの燃費を再計算
    ↓
完了
```

### なぜ「リマーク」機能を実装しないのか

#### 1. 複雑さの削減

リマーク（フラグ付与）ロジックを実装すると：
- 「どのレコードが影響を受けるか」の判定が必要
- 「燃費が妥当かどうか」の閾値判定が必要
- フラグ付きレコードの特別な表示・計算処理が必要
- ユーザーがフラグを理解・管理する認知負荷

#### 2. 満タン法の自己完結性

満タン法は、**現在のデータのみで燃費を計算できる**という特性がある：

```typescript
function calculateFuelEfficiency(history, initOdo) {
  let lastFullTankOdo = initOdo;
  let accumulatedFuel = 0;
  
  for (entry of history) {
    if (entry.skipCalculation) {
      // 記録忘れ: リセット
      lastFullTankOdo = entry.odo;
      accumulatedFuel = 0;
    } else if (!entry.isFullTank) {
      // 部分給油: 積み上げ
      accumulatedFuel += entry.fuel;
    } else {
      // 満タン: 燃費計算
      entry.kmpl = (entry.odo - lastFullTankOdo) / (accumulatedFuel + entry.fuel);
      lastFullTankOdo = entry.odo;
      accumulatedFuel = 0;
    }
  }
}
```

この関数は**履歴の配列だけ**を入力として、各レコードの燃費を計算する。過去のフラグや状態を参照する必要がない。

#### 3. データの整合性

ユーザーがデータを編集・削除した場合、そのデータに基づいて燃費を計算する。

**もし燃費が異常値になった場合:**
- それはユーザーが入力したデータの結果
- ユーザー自身がデータを修正するか、記録忘れフラグを設定する
- システムが勝手にフラグを付けない → ユーザーの意図を尊重

#### 4. 「正しい」vs「シンプル」のトレードオフ

| アプローチ | 正確性 | 複雑さ | ユーザー体験 |
|-----------|--------|--------|-------------|
| 自動リマーク | 高い | 高い | 複雑 |
| データ直接計算 | 中程度 | 低い | シンプル |

**MVP では「シンプル」を優先。** ユーザーが正しいデータを入力すれば、正しい燃費が得られる。

---

## ODOバリデーション

### 日時に基づくODO範囲

記録は日時順にソートされるため、ODOも日時順に増加する必要がある。

```
選択した日時 → 日時順での位置を特定 → 有効なODO範囲を計算
```

#### 例

```
既存データ:
#1: 2026/01/01 10:00, ODO=1000
#2: 2026/01/02 10:00, ODO=1150
#3: 2026/01/03 10:00, ODO=1300

新規入力（日時 = 2026/01/02 15:00）:
位置: #2と#3の間
有効ODO範囲: 1150 < ODO < 1300
```

### 編集時のバリデーション

編集時も同様に、選択した日時での位置に基づいてODO範囲を計算する。

---

## 削除確認ロジック

| 削除対象 | 確認モーダル | 理由 |
|---------|-------------|------|
| 最新レコード | 不要 | 後続レコードへの影響なし |
| それ以外 | 必要 | 後続レコードの燃費が変わる |

---

## 記録タイプ

| タイプ | isFullTank | skipCalculation | 処理 |
|-------|------------|-----------------|------|
| 満タン | true | false | 燃費計算 |
| 部分 | false | false | 燃料積み上げ、燃費なし |
| 記録忘れ | true | true | 燃費計算スキップ、基準点リセット |

### 「記録忘れ」の用途

前回の満タン以降に、記録し忘れた給油があった場合に使用。

```
実際の給油:
1/10: 満タン
1/15: 給油（記録忘れ）
1/20: 満タン ← ここで「記録忘れがある」をON

処理:
1/20 の燃費は計算しない（データ不完全のため）
1/20 のODOを新しい基準点として、次回以降の計算に使用
```

---

## データ構造

```typescript
interface HistoryEntry {
  id: number;
  date: string;           // "2026/03/01 14:30"
  odo: number;            // 総走行距離 (km)
  fuel: number;           // 給油量 (L)
  kmpl: number | null;    // 燃費 (km/L)、計算できない場合は null
  isFullTank: boolean;    // true = 満タン, false = 部分
  skipCalculation: boolean; // true = 記録忘れ
}
```

---

## 制限事項（MVP）

1. データ永続化なし（リロードでリセット）
2. 認証なし
3. 複数バイク非対応
4. カタログ値との比較機能なし
5. 給油リマインダー通知なし

---

## 今後の拡張案

- LocalStorage / IndexedDB によるデータ永続化
- カタログ燃費との比較・メンテナンス促進通知
- 給油スタンド到着時のLocation-based通知
- 複数バイク対応
- データエクスポート（CSV）
