# 給油記録・燃費表示機能 仕様書

## 画面構成

### 1. Initial Setup（初回セットアップ）

初回起動時に以下を収集する：

| ステップ | 入力項目          | 備考                 |
| -------- | ----------------- | -------------------- |
| 1        | ODO（総走行距離） | 現在のメーター表示値 |
| 2        | 燃費の表示単位    | km/L または L/100km  |

> ※ 車種情報・カタログ燃費はアカウント登録時に取得済みのため、Setup での入力は不要。

---

### 2. ダッシュボード

「自分のバイクがいかに経済的か」を一目で感じられる画面。ODOの数値そのものは表示せず、燃費・費用・走行感の観点から訴求する。

**表示内容：**

- 累積平均燃費 avg(H)（km/L）
- カタログ値との比較ライン
  - avg(H) > カタログ値 → 「お客様のバイクは非常に効率よく走れています！🎉」
  - avg(H) < カタログ値（継続的）→ 「最近燃費が低下しています。メンテナンスをお勧めします」
- 給油ごとの燃費推移グラフ
- 給油ごとの支払金額の推移グラフ
- 累計給油回数・累計費用などのサマリー

Honda バイクなら「こんなに少ない出費でこれだけ走れている」ことを可視化することで、低燃費性能を体感させる。

---

### 3. 実現する機能

| 機能                         | 内容                                                                 |
| ---------------------------- | -------------------------------------------------------------------- |
| 1区間あたりの燃費            | H = ΔODO ÷ L　各レコードに表示                                       |
| 合計の燃費                   | avg(H) cumulative = (現在ODO − 初回ODO) ÷ 累計L                      |
| 記録忘れ区間の除外           | flaggedレコードはkm/L計算から除外。ODOのみ保存し次回のbaselineとする |
| 記録削除時の警告・自動フラグ | 下記参照                                                             |

#### 記録削除のルール

**最新レコードの削除：**

- 強めの警告を表示後、削除可能

**最新以外のレコードの削除：**

- 強めの警告を表示
- 削除後、隣接する1つ新しいレコードのΔODOを再計算し、燃費の妥当性を自動チェックする

**チェック方法：**
再計算後の燃費（ΔODO_new ÷ 給油量L）をカタログ値と比較する。

- 乖離が **30%以内** → データは正常とみなし、フラグなしで燃費を算出 ✅
- 乖離が **30%超** → 記録忘れの可能性があるとみなし、自動で給油忘れフラグを付与。燃費は算出せずODOのみ保存 ⚠️

> ※ 閾値30%は記録忘れ検知と共通の仮値。Honda側と実データをもとに調整が必要。

---

### 4. 給油記録入力

#### ステップ 1 — ODO入力

- 現在のメーター表示値を入力
- 前回のODOを参照表示（読み取り専用）
- バリデーション：
  - 前回より小さい値 → エラー表示
  - 前回から異常に大きい距離（例：1,500km超）→ 確認アラート

#### ステップ 2 — 給油量入力

- リットル直接入力
- または支払金額入力（単価を元に自動換算）
- 日時：現在時刻で自動入力（時刻まで記録。1日複数回の給油に対応）

#### ステップ 3 — 記録忘れ確認（条件付き表示）

**常に表示するのではなく、以下の条件を満たした場合のみシステムから提案する：**

- 今回の km/L が過去平均と比較して 30% 以上乖離している場合
  → 「前回の給油後、記録し忘れた給油はありましたか？」を表示

| 回答                 | 処理                                                      |
| -------------------- | --------------------------------------------------------- |
| いいえ（記録は正確） | 通常通り燃費を算出・保存                                  |
| はい（記録忘れあり） | 案1: L推定で参考値算出 / 案2: ODOのみ保存・基準値リセット |

> ※ 閾値30%は仮値。Honda側と実データをもとに調整が必要。

---

## 背景・目的

Hondaは自社バイクの低燃費性能をユーザーに実感してもらうことを目指している。また、ユーザーの走行距離データを継続的に収集し、ディーラーや MA からの適切なタイミングでのアクション（点検通知・再来店促進など）に活用したい。
本提案では、「給油記録・燃費表示機能」をその実現手段として設計する。

---

## 設計の前提

### ユーザー側の動機

ユーザーがすべての給油を빠짐없く記録し続ける可能性は高くない。そのため、計算の正確性よりも「継続的に入力したくなる体験」を優先する。燃費（km/L）の可視化は、ユーザーが入力を続けるための主な動機づけとして機能させる。

### ODO（総走行距離）の扱い方針

ユーザーが入力したODOは販促目的のみに使用する。アプリ上でODOの数値をそのまま表示・比較することはしない。

| 活用先                 | 内容                                                                      |
| ---------------------- | ------------------------------------------------------------------------- |
| Salesforce（MA）       | 条件に応じてアプリへ自動Push通知（例：累積走行距離が3,000km超で点検通知） |
| ディーラー＋Salesforce | 前回メンテODOとの差分が閾値を超えた場合、電話・メールでフォロー           |

---

## 入力・表示データ仕様

### 給油記録の入力項目

| 項目                      | 必須/任意 | 備考                                                          |
| ------------------------- | --------- | ------------------------------------------------------------- |
| 現時点の総走行距離（ODO） | 必須      | 手動入力。将来的に画像認識での入力も検討                      |
| 給油量（L）               | 必須      | リットル直接入力、または総額 ÷ 単価で自動換算                 |
| 日時                      | 必須      | 現在時刻で自動入力。1日複数回の給油に対応するため時刻まで記録 |
| 給油忘れチェック          | 必須      | True/False、デフォルトはFalse                                 |
| 総額                      | 任意      | Honda側として取得したい情報                                   |
| 1Lあたりの金額            | 任意      | 給油量の自動換算に使用                                        |

### 表示のみのデータ

- 前回の総走行距離（ODO）

### 実現する機能

- 1区間あたりの燃費（km/L）
- 合計の燃費（累積 avg(H)）
- 記録忘れ区間の燃費計算からの除外
- 記録削除時の強めの警告 → 削除した場合、隣接する新しい記録に自動で給油忘れフラグを付与

---

## 記録忘れ対応：2案比較

### 案1 — トリップメーターHを用いた参考値算出 ⭐ Phase 2推奨

#### 概要

Hondaバイクに搭載されているトリップメーター（H）を活用し、記録忘れが発生した区間の給油量を推定して「参考値」として算出する。

#### 前提となる関係式

バイクのトリップメーターHは以下の式で定義される：

```
H = ΔODO ÷ L
```

- **ΔODO**：ユーザーがメーターから直接入力 → 信頼できる値
- **H**：バイクが自動計算して表示 → 信頼できる値
- **L**：記録忘れの場合は信頼できない → 逆算で補完

```
L = ΔODO ÷ H
```

記録忘れ時は上記式でLを推定し、燃費算出に使用する。

#### 算出ロジック例

| 日付          | ODO  | ΔODO | 給油量 L                   | 今回燃費 H           | avg(H)                                          |
| ------------- | ---- | ---- | -------------------------- | -------------------- | ----------------------------------------------- |
| initial (1/4) | 1000 | —    | —                          | —                    | 50（カタログ）                                  |
| 1/8           | 1130 | 130  | 2.5                        | 52                   | 50（カタログ）                                  |
| **1/13**      | —    | —    | **記録忘れ**               | —                    | —                                               |
| 1/25          | 1500 | 500  | ~~2~~ → **10**（推定）     | 500÷10 = **50**      | 50（カタログ）                                  |
| 2/10          | 1600 | 100  | 1.8                        | 55.56                | 50（カタログ）                                  |
| 2/15          | 1800 | 200  | 3.8                        | 52.63                | 50（カタログ）                                  |
| 2/28          | 1920 | 120  | 2.3                        | 52.17                | 50（カタログ）                                  |
| 3/4           | 2100 | 180  | 3.5                        | 51.42                | **(2100−1000)÷(10+1.8+3.5+2.3+3.5) = 52.13** ✅ |
| **3/7**       | —    | —    | **記録忘れ**               | —                    | —                                               |
| 3/12          | 2400 | 300  | ~~1.8~~ → **5.75**（推定） | 300÷5.75 = **52.13** | 52.13                                           |

**1/25 の推定処理：**

```
ΔODO = 1500 − 1000 = 500km
L推定 = ΔODO ÷ H_catalog = 500 ÷ 50 = 10L
今回燃費 = 500 ÷ 10 = 50 km/L（参考値）
```

**3/12 の推定処理：**

```
ΔODO = 2400 − 2100 = 300km
L推定 = ΔODO ÷ avg(H) = 300 ÷ 52.13 = 5.75L
今回燃費 = 300 ÷ 5.75 = 52.13 km/L（参考値）
```

> ※ 初回は H_catalog（カタログ値）を基準に使用。記録が蓄積された後は avg(H) を使用する。

#### avg(H) — 累積燃費の算出

単純平均ではなく、初回ODOからの累積値で算出する：

```
avg(H) = (現在のODO − 初回ODO) ÷ 累計給油量L
```

例（3/4時点）：

```
(2100 − 1000) ÷ (10 + 1.8 + 3.5 + 2.3 + 3.5) = 52.13 km/L
```

記録が増えるほど安定し、信頼性が向上する。

#### カタログ値との比較によるメンテナンス促進

- avg(H) > カタログ値 → 「お客様のバイクは非常に効率よく走れています！🎉」
- avg(H) < カタログ値（継続的に下回る）→ 「最近燃費が低下しています。メンテナンスをお勧めします」→ ディーラー来店を促すPush通知

#### グラフ・一覧での表示

システムによる推定値であることを明示するため、通常の記録とは異なるスタイル（破線・薄い色）でグラフや一覧に残す。

#### メリット

- グラフに空白が生じず、視覚的に連続性が保たれる
- ユーザーが燃費の傾向を把握しやすい
- カタログ値との比較でメンテナンス需要を検知し、ディーラーへの来店促進に活用できる
- 推定値はバイク搭載データ（H）に基づくため、mean imputation（平均値補完）より客観的

#### デメリット

- カタログ値と実走行の乖離がある場合、推定精度が低くなる
- Honda側からモデルごとのカタログ燃費データの提供が必要
- 実装複雑度が高い

#### 前提条件

- Honda から車種ごとのカタログ燃費（km/L）の提供が必要
- ユーザーが初回セットアップ時に車種を登録する必要がある

---

### 案2 — ODOのみ記録・基準値リセット ⭐ Phase 1推奨

#### 概要

記録忘れが発生した場合、燃費の算出は行わない。ODOの記録のみを保存し、次回以降の計算の基準値として扱う。初回セットアップと同じ扱い。

#### 処理ロジック例

```
状況：
  1/10: ODO 1000km（記録済み）
  1/13: 給油あり（記録忘れ）
  1/25: ODO 1500km、給油量 15L、給油忘れ = True

処理：
  1/25のレコードは燃費計算から除外
  ODO 1500km を新しい基準値として設定
  2/8以降の記録から燃費算出を再開
```

#### グラフ・一覧での表示

記録忘れフラグが立っている区間は ⚠️ マークを表示し、グラフには含めない。

#### メリット

- 実装がシンプル
- 誤った燃費データをユーザーに見せるリスクがない
- Honda側からのカタログデータ提供が不要
- 車種登録が不要

#### デメリット

- 記録忘れのたびにグラフに空白が生じる
- 頻繁に記録を忘れるユーザーはグラフがほぼ表示されない

---

## 2案の比較まとめ

| 観点             | 案1（トリップメーターH活用） | 案2（ODOのみ記録）   |
| ---------------- | ---------------------------- | -------------------- |
| 実装複雑度       | 高い                         | 低い                 |
| データ精度       | Hに基づく推定（客観的）      | 正確（計算しない）   |
| Honda側の準備    | カタログデータ提供が必要     | 不要                 |
| 車種登録         | 必要                         | 不要                 |
| グラフの連続性   | 保たれる                     | 空白が生じる         |
| ユーザー体験     | データが埋まり見やすい       | シンプルで誤解がない |
| メンテナンス促進 | カタログ値比較で自動検知     | なし                 |
| リリース優先度   | Phase 2推奨                  | Phase 1推奨          |

---

## 記録忘れ確認のトリガー

常にユーザーに確認を求めるのではなく、システムが燃費の異常を検知した場合のみ確認を促す設計とする。

```
トリガー条件（例）：
  今回の km/L が過去平均と比較して 30% 以上乖離している場合
  → 「前回の給油後、記録し忘れた給油はありましたか？」を表示
```

通常時はこの確認画面をスキップすることで、入力ステップを最小化しユーザーの負担を下げる。

> ⚠️ 注意：閾値30%はあくまで仮値。Honda側と実データをもとに調整が必要。

---

## 通知トリガー（検討中）

### 基本案（実装容易）

ユーザーが指定した時刻に毎日Push通知を送信する。通知時刻はユーザーが設定画面から変更可能。

### 発展案（要技術確認）

給油スタンド付近に到達した際に通知を送信するLocation-based triggerも検討に値する。ただし以下の点について技術・UX両面での確認が必要：

- iOSおよびAndroidにおけるバックグラウンドでのGeofencing可否
- 位置情報の常時取得に対するユーザーの許諾ハードル
- バッテリー消費への影響

当面は基本案で実装し、Location-based triggerは次フェーズでの検討事項とする。
